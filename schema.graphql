# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
}

type Collection {
    id: ID! # ! means that there will always be ID property in Collection
    items: [Item!]!
    title: String!
}

type Item {
    collection: Collection #this is the owner of this Item type
    id: ID!
    imageUrl: String!
    name: String!
    price: Float!
}

type Query { #you can query of collection, collections, and getCollectionByTitle
    collection(id: ID!): Collection #because this doesn't always neccessarily returns a collection # this requests will return a data object with the property of the query name e.g getCollectionsByTitle
    collections: [Collection!]!
    getCollectionsByTitle(title: String): Collection
}
